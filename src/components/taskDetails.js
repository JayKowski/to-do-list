export default function TaskDetails(name, date, priority, completed, leftDays) {
  this.taskName = name;
  this.dueDate = date;
  this.priority = priority;
  this.completed = completed;
  this.leftDays = leftDays;
}
